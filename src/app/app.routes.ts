// src/app/app.routes.ts
// Archivo de rutas - por ahora solo tenemos una página principal

import { Routes } from '@angular/router';

// Por ahora no tenemos rutas porque solo hay una página
export const routes: Routes = [];
